<?xml version="1.0" encoding="utf-8"?>
<mdscript name="One_Sector" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../../../../xsd/md.xsd">

  <!-- todo:
  update variables and props

  aiscripts:
    size_s ships buy supplies by the unit instead of by the container
    resupply ships buy supplies by the container, and then deliver them to ships which consume them

  planet stations refill with supplies
  -->

  <cues>
    <cue name="One" namespace="this" version="1" mapeditor="false">
      <conditions>
        <check_any>
          <event_cue_signalled cue="md.Setup.GameStart" />
          <event_player_created />
        </check_any>
      </conditions>
      <actions>
      </actions>
      <cues>
        <cue name="LuaEvents_Instanced" namespace="this" instantiate="true">
          <conditions>
            <check_any>
              <event_cue_signalled /> <!-- param.$event param.$Ship -->
              <event_ui_triggered screen="'OneSector'" control="'setDemandProduction'" /> <!-- param3 = table[products[ware, value], resources[ware, value]] -->
            </check_any>
          </conditions>
          <actions>
            <do_if value="event.param2 == 'setDemandProduction'">

            </do_if>
          </actions>
        </cue>
      </cues>
    </cue>
  </cues>
</mdscript>